@{
}



<div class="container d-flex justify-content-center">
    <div class="col-md-4">
        <h1>Şifremi Unuttum</h1>
        <form asp-controller="Account" asp-action="SendEmailForgotPassword" method="post">
            <div class="form-group">
                <label for="emailOrUserName">Mail Adresi / Kullanıcı Adı</label>
                <input class="form-control" name="emailOrUserName" id="emailOrUserName" required minLength="3" value="@TempData["emailOrUserName"]"/>
            </div>
            <div class="form-group mt-2">
                <button class="btn btn-outline-info w-100">Şifremi Değiştirme Kodu Gönder</button>
            </div>
        </form>
        @if (TempData["error"] != "" && TempData["error"] != null)
        {
            <p class="alert alert-danger mt-2">@TempData["error"]</p>
        }
    </div>
</div>